# Fail2ban filter for ffmpeg-gui
# Matches standardized [security] failed login attempt warnings
# Emitted in ffmpeg-gui.log or journald/syslog

[Definition]
failregex = ^.*\[security\] Failed GUI login attempt from <HOST>
ignoreregex =
